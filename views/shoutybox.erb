<html>
    <head> </head>
    <body>

    <h1>SHOUTYBOX</h1>
      
      <% @peeps.each do |peep| %>
        <%=@users.find(peep.user_id).name%> - <%=@users.find(peep.user_id).username%> -
        <%=peep.time%>
        <h2><%=peep.peep%></h2><br>
      <% end %>
        
    </body>

</html>
